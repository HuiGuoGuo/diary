<%= form_for(@attachment_image) do |f| %>
  <% if @attachment_image.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@attachment_image.errors.count, "error") %> prohibited this attachment_image from being saved:</h2>

      <ul>
      <% @attachment_image.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :attachment_image_file_name %><br />
    <%= f.text_field :attachment_image_file_name %>
  </div>
  <div class="field">
    <%= f.label :attachment_image_content_type %><br />
    <%= f.text_field :attachment_image_content_type %>
  </div>
  <div class="field">
    <%= f.label :attachment_image_file_size %><br />
    <%= f.number_field :attachment_image_file_size %>
  </div>
  <div class="field">
    <%= f.label :attachment_image_updated_at %><br />
    <%= f.datetime_select :attachment_image_updated_at %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
